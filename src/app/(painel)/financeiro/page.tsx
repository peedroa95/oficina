import {prisma} from '@/lib/prisma';import {createTransaction} from '@/app/actions';import {money,dateBR} from '@/lib/format';import {PageTitle} from '@/components/page-title';export const dynamic='force-dynamic';
export default async function Page(){const txs=await prisma.financialTransaction.findMany({orderBy:{date:'desc'},take:100});const income=txs.filter(t=>t.type==='ENTRADA').reduce((s,t)=>s+Number(t.amount),0),expense=txs.filter(t=>t.type==='SAIDA').reduce((s,t)=>s+Number(t.amount),0);return <><PageTitle title="Financeiro" description="Controle simples de entradas e saídas."/><div className="mb-5 grid gap-4 sm:grid-cols-3"><div className="card p-5"><p className="text-sm text-slate-500">Entradas</p><b className="text-xl text-green-600">{money(income)}</b></div><div className="card p-5"><p className="text-sm text-slate-500">Saídas</p><b className="text-xl text-red-600">{money(expense)}</b></div><div className="card p-5"><p className="text-sm text-slate-500">Saldo</p><b className="text-xl">{money(income-expense)}</b></div></div><details className="card mb-5 p-5"><summary className="cursor-pointer font-bold text-blue-600">+ Novo lançamento</summary><form action={createTransaction} className="mt-4 grid gap-3 md:grid-cols-4"><input required name="description" className="field md:col-span-2" placeholder="Descrição *"/><select name="type" className="field"><option value="ENTRADA">Entrada</option><option value="SAIDA">Saída</option></select><input required name="amount" min="0.01" step="0.01" type="number" className="field" placeholder="Valor"/><select name="category" className="field"><option>Venda de serviços</option><option>Venda de peças</option><option>Compra de peças</option><option>Aluguel</option><option>Energia</option><option>Água</option><option>Internet</option><option>Funcionários</option><option>Outros</option></select><select name="paymentMethod" className="field"><option>Dinheiro</option><option>PIX</option><option>Cartão de crédito</option><option>Cartão de débito</option><option>Transferência</option></select><input required name="date" type="date" defaultValue={new Date().toISOString().slice(0,10)} className="field"/><input name="notes" className="field" placeholder="Observação"/><button className="btn btn-primary">Salvar lançamento</button></form></details><div className="card table-wrap"><table className="table"><thead><tr><th>Data</th><th>Descrição</th><th>Categoria</th><th>Forma</th><th>Valor</th></tr></thead><tbody>{txs.map(t=><tr key={t.id}><td>{dateBR(t.date)}</td><td className="font-bold">{t.description}</td><td>{t.category}</td><td>{t.paymentMethod||'—'}</td><td className={`font-bold ${t.type==='ENTRADA'?'text-green-600':'text-red-600'}`}>{t.type==='SAIDA'?'- ':'+ '}{money(t.amount.toString())}</td></tr>)}</tbody></table></div></>}
