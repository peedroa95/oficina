import {prisma} from '@/lib/prisma';import {createService} from '@/app/actions';import {money} from '@/lib/format';import {PageTitle} from '@/components/page-title';import {Plus} from 'lucide-react';export const dynamic='force-dynamic';
export default async function Page(){const services=await prisma.service.findMany({orderBy:{name:'asc'}});return <><PageTitle title="Serviços" description="Catálogo de serviços e preços padrão."/><details className="card mb-5 p-5"><summary className="cursor-pointer font-bold text-blue-600"><Plus className="mr-2 inline" size={18}/>Novo serviço</summary><form action={createService} className="mt-5 grid gap-4 md:grid-cols-4"><input required name="name" className="field" placeholder="Nome do serviço *"/><input name="description" className="field md:col-span-2" placeholder="Descrição"/><input required name="defaultPrice" type="number" min="0" step="0.01" className="field" placeholder="Valor padrão"/><label className="flex items-center gap-2"><input defaultChecked type="checkbox" name="active"/> Serviço ativo</label><button className="btn btn-primary">Salvar serviço</button></form></details><div className="card table-wrap"><table className="table"><thead><tr><th>Serviço</th><th>Descrição</th><th>Valor padrão</th><th>Status</th></tr></thead><tbody>{services.map(s=><tr key={s.id}><td className="font-bold">{s.name}</td><td>{s.description||'—'}</td><td>{money(s.defaultPrice.toString())}</td><td><span className={`badge ${s.active?'bg-green-100 text-green-700':'bg-slate-100 text-slate-600'}`}>{s.active?'Ativo':'Inativo'}</span></td></tr>)}</tbody></table></div></>}
