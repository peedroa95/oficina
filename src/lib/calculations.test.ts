import {describe,it,expect} from 'vitest';import {orderTotal,paymentSituation} from './calculations';describe('cálculos da oficina',()=>{it('soma serviços e peças e aplica desconto',()=>expect(orderTotal([{quantity:2,unitPrice:100}],[{quantity:1,unitPrice:50}],20)).toBe(230));it('não permite total negativo após desconto',()=>expect(orderTotal([{quantity:1,unitPrice:10}],[],20)).toBe(0));it('calcula estado financeiro',()=>{expect(paymentSituation(100,0)).toBe('PENDENTE');expect(paymentSituation(100,50)).toBe('PARCIAL');expect(paymentSituation(100,100)).toBe('PAGO')})});
