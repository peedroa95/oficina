'use client';
import {Search,Bell} from 'lucide-react';import {useState} from 'react';import Link from 'next/link';
export function Header({name,role}:{name:string;role:string}){const [q,setQ]=useState('');return <header className="no-print sticky top-0 z-20 flex h-20 items-center border-b bg-white px-5 pl-16 md:pl-8"><form action="/busca" className="relative w-full max-w-xl"><Search className="absolute left-3 top-2.5 text-slate-400" size={20}/><input name="q" value={q} onChange={e=>setQ(e.target.value)} className="field pl-10" placeholder="Buscar cliente, placa ou número da OS..."/></form><div className="ml-auto flex items-center gap-4 pl-4"><Bell size={20}/><div className="hidden text-right sm:block"><b className="block text-sm">{name}</b><span className="text-xs text-slate-500">{role==='ADMINISTRADOR'?'Administrador':'Funcionário'}</span></div><div className="grid h-10 w-10 place-items-center rounded-full bg-blue-100 font-bold text-blue-700">{name.charAt(0)}</div></div></header>}
